/**
 * API Auth static class
 */
export default class ApiAuth
{
    static login(action)
    {
        let response = [];

		// Mock
		return {
			status: 1
		}
    }

	static signup(action)
	{
		let response = [];

		// Mock
		return {
			status: 1
		}
	}

    static logout(action)
    {
        let response = [];

        // Mock
        return {
            status: 0
        }
    }
}
